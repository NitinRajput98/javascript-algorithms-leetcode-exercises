function addItems(n) {
    return n + n
}

addItems(10)

// The above function has a time complexity of O(1) because it performs a constant amount of work regardless of the input size n. The function simply adds n to itself, which takes the same amount of time no matter how large n is. Therefore, the time complexity is O(1).